[project]
name = "ha-doglog"
version = "0.1.0"
requires-python = ">=3.12"

[project.optional-dependencies]
dev = ["pytest>=8.0", "requests>=2.31"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
